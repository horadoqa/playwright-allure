# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: api/healthcheck.spec.js >> HealthCheck >> should return 200 OK when accessing serverest.dev
- Location: tests/api/healthcheck.spec.js:5:7

# Error details

```
Error: apiRequestContext.get: First argument must be either URL string or Request
```

# Test source

```ts
  1  | import { test, expect } from '@playwright/test';
  2  | import 'dotenv/config';
  3  | 
  4  | test.describe('HealthCheck', () => {
  5  |   test('should return 200 OK when accessing serverest.dev', async ({ request }) => {
> 6  |     const response = await request.get(process.env.URL);
     |                                    ^ Error: apiRequestContext.get: First argument must be either URL string or Request
  7  | 
  8  |     expect(response.status()).toBe(200);
  9  |   });
  10 | });
```